## fixed `qtcreator` and `codeblocks` backends

The error of "bad backend generation" of `qtcreator` and `codeblocks`. Has been fixed,
it is now posable to generate your project and not have missing info.
